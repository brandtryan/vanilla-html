// Functions for selecting part of document to animate

// Animate Section

export function selectContent(id) {
	const element = document.getElementById(`${id}`);
	console.log(element.textContent);
}
